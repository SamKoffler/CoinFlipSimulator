import random

heads = 0
tails = 0

user_amount = int(input("Enter number of coin flips: "))

for i in range(user_amount):
        x = random.randint(1,2)
        if x==1:
            heads+=1
        else:
            tails+=1


print(f"Heads: {heads} - Tails: {tails}")
